当前位置->修改个人资料
<%= form_for @current_user, :url => {:action=>'update'} do |f| %>
  <% if @current_user.errors.any? %>
    <div>
      <h2>
        <%= pluralize(@current_user.errors.count, "error") %> prohibited this article from being saved:
      </h2>
      <ul>
        <% @current_user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="table-body">
  <table>
    <tr>
      <td><%= f.label :login, "账户名" %></td>
      <td><%= f.text_field :login ,:readonly => "readonly"%></td>
    </tr>
    <tr>
      <td><%= f.label :email, "邮箱" %></td>
      <td><%= f.text_field :email %></td>
    </tr>
    <tr>
      <td><%= f.label :password, "密码" %></td>
      <td><%= f.password_field :password %></td>
    </tr>
    <tr>
      <td><%= f.label :password_confirmation, "确认密码" %></td>
      <td><%= f.password_field :password_confirmation %></td>
    </tr>
    <tr>
      <td><%= f.label :nickname, "昵称" %></td>
      <td><%= f.text_field :nickname %></td>
    </tr>
    <tr>
      <td></td>
      <td><%= f.submit "提交" %></td>
    </tr>
  </table>
<% end %>
</div>
<style>
  .table-body{
    margin: 100px;
    /*border: solid 1px black;*/
  }
  .table-body table{
    border: black 1px solid;
    width: 100%;
    mso-cellspacing: 0;
    text-align: center;
    border-collapse: collapse;
    line-height: 30px;
  }
  .table-body table td{
    border: black 1px solid;
    margin: 0px;
    padding: 0px;
  }
</style>