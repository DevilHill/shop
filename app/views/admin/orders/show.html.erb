<%= link_to "添加商品" ,new_admin_order_product_path(:order_id => params[:id]) %>
<div>
  <table class="">
    <tr>
      <td>商品名称</td>
      <td>操作</td>
    </tr>
    <% @order_product.each do |o| %>
      <tr>
        <% product = Product.find_by_id(o.product_id) %>
        <td><%= product.name %></td>
        <td><% link_to "删除"  %></td>
      </tr>
    <% end %>
  </table>
</div>